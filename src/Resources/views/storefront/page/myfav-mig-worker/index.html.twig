{% sw_extends '@Storefront/storefront/base.html.twig' %}

{% block base_header %}
{% endblock %}

{% block base_navigation %}
{% endblock %}

{% block base_main %}
    <div class="container-main">
        <div class="container">
            {% if message|length > 0 %}
                <div class="alert alert-success" role="alert" style="border: 1px solid #333; padding: 6px 12px; color: #FFF;">
                    {#
                    {% if message === 'myfavMigCreated' %}
                        Das Helper Setup wurde angelegt.
                    {% else %}
                        Unbekannte Erfolgsmeldung
                    {% endif %}
                    #}
                </div>
            {% endif %}
            <h1>Migration Helper - Worker</h1>
            <div class="myfav-mig">
                <div class="myfav-mig mb-3 mt-3" style="font-size: 1rem;">
                    Name: {{ myfavMig.name }}<br />
                    ID: {{ myfavMig.id }}<br />
                    Pos.: <span id="myfav-mig-worker-pos">{{ myfavMig.pos}}</span><br />
                    State: <span id="myfav-mig-worker-state">{{ myfavMig.state}}</span><br /><br />
                    <a href="#"><button type="submit" class="btn btn-primary" id="myfav-mig-worker-start">Verarbeitung starten/fortsetzen</button></a>
                </div>
                <hr />
                Log:<br />
                <br />
                <div id="myfav-mig-worker-log"></div>
            </div>
        </div>
    </div>
{% endblock %}

{% block base_cookie_permission %}
{% endblock %}

{% block base_footer %}
{% endblock %}